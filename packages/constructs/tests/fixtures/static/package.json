{
  "name": "@astrojs-aws/constructs-fixtures-static",
  "private": true,
  "scripts": {
    "build": "pnpm run clean && astro build",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "astro": "^2.2.1",
    "rimraf": "^4.4.1"
  }
}
